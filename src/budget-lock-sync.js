// Keep Bible budget comparisons tied to the current physical location and aggregate each vendor's full Budget footprint.
import { configured, getShowId, getLocationId, loadBudget, loadLocations } from './supabase.js';
const showId=getShowId(),locationId=getLocationId();let budgetCandidates=[];
const norm=x=>String(x||'').trim().toLowerCase().replace(/\s+/g,' ');
function calc(i){if(!i)return 0;if(i.calcType==='flat')return Number(i.flatAmount||0);if(i.calcType==='rateDay')return Number(i.days||0)*Number(i.dayRate||0);if(i.calcType==='hourly'){const p=+i.people||0,d=+i.days||0,r=+i.hourlyRate||0,reg=+i.regHours||0,ot=+i.ot15Hours||0,dt=+i.ot2Hours||0,kit=+i.kitFee||0;return p*d*(reg*r+ot*r*1.5+dt*r*2)+(i.kitFeeMode==='flat'?kit:p*d*kit)}if(i.calcType==='dayRate'){const p=+i.people||0,d=+i.days||0,rate=+i.dayRate||0,inc=Math.max(+i.includedHours||12,1),hr=rate/inc;return p*d*rate+p*d*((+i.ot15Hours||0)*hr*1.5+(+i.ot2Hours||0)*hr*2)+(i.kitFeeMode==='flat'?(+i.kitFee||0):p*d*(+i.kitFee||0))}if(i.calcType==='vendor'){const units=+i.units||0,base=i.vendorBillingType==='flat'?units*(+i.vendorFlatRate||0):units*(+i.weeks||0)*(+i.weeklyRate||0);return base+units*(+i.servicesPerUnit||0)*(+i.serviceRate||0)+(+i.flatAmount||0)}return 0}
const generic={security:['security','guard','gaffer'],restrooms:['restroom','toilet'],cleaning:['clean'],bins:['bin','dumpster','trash','waste'],catering:['catering','lunchbox'],snake:['snake','wrangler'],maps:['map']};
function plannerVendor(panel){const shell=panel.closest('.vendor-planner-shell')||document;const selected=shell.querySelector('select option:checked')?.textContent||'';const head=shell.querySelector('h1,h2')?.textContent||'';const sub=shell.querySelector('h1,h2')?.nextElementSibling?.textContent||'';return norm(`${selected} ${head} ${sub}`)}
function aliases(id,panel){if(id==='equipment'){const v=plannerVendor(panel);if(/hollywood depot|\bhdr\b/.test(v))return ['hollywood depot','hdr'];if(/air on location/.test(v))return ['air on location'];return ['equipment']}return generic[id]||[id]}
function allItems(){return budgetCandidates.flatMap(b=>b.items||[])}
function locked(id,panel){const ks=aliases(id,panel),seen=new Set();return allItems().filter(i=>{const key=i.id||`${i.name}|${i.vendor}|${i.sectionId}`;if(seen.has(key))return false;const t=norm(`${i.name||''} ${i.vendor||''}`),match=ks.some(k=>t.includes(k));if(match)seen.add(key);return match}).reduce((n,i)=>n+calc(i),0)}
function money(n){return new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number(n||0))}
function updatePanels(root=document){root.querySelectorAll?.('[data-budget-card]').forEach(panel=>{const amount=locked(panel.dataset.budgetCard,panel);panel.dataset.lockedBudget=String(amount);const strong=panel.querySelector('.compare-block.locked strong');if(strong)strong.textContent=money(amount);const working=Number((panel.querySelector('.working-total')?.textContent||'0').replace(/[^0-9.-]/g,''))||0,variance=working-amount,out=panel.querySelector('.variance-total'),note=panel.querySelector('.variance-note');if(out)out.textContent=money(variance);if(note)note.textContent=variance<=0?'Under budget':'Over budget'})}
async function init(){if(!configured||!showId)return;try{const [budgetDoc,locations]=await Promise.all([loadBudget(showId),loadLocations(showId)]),budgets=budgetDoc?.payload?.budgets||[],here=locations.find(x=>x.id===locationId),name=norm(here?.location_name),set=norm(here?.set_name),ep=norm(here?.episode_name||here?.episode_id);budgetCandidates=budgets.filter(b=>b.sharedLocationId===locationId||(name&&norm(b.location)===name)||(set&&norm(b.setName)===set&&(!ep||norm(b.episode)===ep)));if(!budgetCandidates.length)budgetCandidates=budgets.filter(b=>name&&norm(b.location)===name);updatePanels(document);new MutationObserver(ms=>ms.forEach(m=>m.addedNodes.forEach(n=>n.nodeType===1&&queueMicrotask(()=>updatePanels(n))))).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('change',e=>{if(e.target.matches?.('select'))queueMicrotask(()=>updatePanels(document))},true)}catch(e){console.warn('Could not sync locked Budget values',e)}}
await init();
